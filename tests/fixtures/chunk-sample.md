# Sample doc for chunking

Intro paragraph that is long enough to become its own chunk when split by H2 headers in the indexer.

## First Section

This section has enough text content to exceed the minimum chunk length threshold used by chunkMarkdownByH2.

## Second Section

Another section with sufficient body text for the chunker to emit a separate chunk entry.
