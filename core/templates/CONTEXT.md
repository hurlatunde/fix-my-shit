# Phase ${PHASE} — Context

**Phase:** ${PHASE_NAME}
**Created:** ${DATE}

## Domain

(Primary technology or problem domain for this phase.)

## Decisions

Locked choices — planner and executor MUST implement exactly as specified.

- (e.g. "Use library X for auth")
- (e.g. "Card layout, not table")

## Claude's Discretion

Areas where the AI agent can use judgment. Document choices in the plan.

- (e.g. "Choose folder structure")
- (e.g. "Pick test framework")

## Deferred Ideas

Out of scope for this phase — MUST NOT appear in plans.

- (e.g. "Search functionality — future phase")
- (e.g. "Dark mode — not in v1")

## Canonical References

External specs, ADRs, or documentation the planner should read.

- (e.g. `docs/api-spec.yaml`)
- (e.g. `ADR-003-auth-strategy.md`)

## Specifics

Concrete requirements, constraints, or acceptance criteria for this phase.

- (e.g. "Must support email + password login")
- (e.g. "Response time < 200ms for API endpoints")

---
*Created by discuss-phase workflow*
